/**
 * @athena/branding — product identity + assets.
 * Runtime resolution also lives in `@athena/shared` for Next.js/Nest consumers.
 */
export {
  MOVVO_PRODUCT,
  ATHENA_ACADEMIA_BRANDING,
  ATHENA_PRODUCT_NAME,
  ATHENA_PRODUCT_VERSION,
  resolveCompanyBranding,
} from '@athena/shared';

export type { ResolvedCompanyBranding } from '@athena/shared';
