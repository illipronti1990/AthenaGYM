/**
 * @movvo/branding — product identity + assets.
 * Runtime resolution also lives in `@movvo/shared` for Next.js/Nest consumers.
 */
export {
  MOVVO_PRODUCT,
  ATHENA_ACADEMIA_BRANDING,
  resolveCompanyBranding,
} from '@movvo/shared';

export type { ResolvedCompanyBranding } from '@movvo/shared';
