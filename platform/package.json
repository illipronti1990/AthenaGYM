{
  "name": "athena-platform",
  "private": true,
  "version": "0.9.0",
  "description": "ATHENA PLATFORM — SaaS (Sprint 8 BI Analytics)",
  "scripts": {
    "ports:free": "node scripts/free-ports.js",
    "dev": "pnpm --parallel --filter @athena/api --filter @athena/web dev",
    "dev:fresh": "pnpm ports:free && node -e \"require('fs').rmSync('apps/web/.next',{recursive:true,force:true})\" && pnpm dev",
    "dev:api": "pnpm --filter @athena/api dev",
    "dev:web": "pnpm --filter @athena/web dev",
    "dev:worker": "pnpm --filter @athena/worker start:dev",
    "dev:all": "pnpm --parallel --filter @athena/api --filter @athena/web --filter @athena/worker dev",
    "build": "pnpm -r run build",
    "build:packages": "pnpm -r --filter \"./packages/*\" run build",
    "build:apps": "pnpm --filter @athena/api --filter @athena/web run build",
    "build:api:prod": "pnpm --filter @athena/api... run build",
    "build:web:prod": "pnpm --filter @athena/shared build && pnpm --filter @athena/web build",
    "check:deploy-env": "node scripts/check-deploy-env.mjs",
    "lint": "pnpm -r run lint",
    "start:api": "pnpm --filter @athena/api start",
    "start:api:prod": "pnpm --filter @athena/api start:prod",
    "start:web": "pnpm --filter @athena/web start",
    "test:api": "pnpm --filter @athena/api test",
    "test:e2e": "playwright test",
    "lighthouse:m4": "pnpm --filter @athena/web lighthouse:m4",
    "test": "pnpm test:api"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^26.1.1",
    "sharp": "^0.35.3",
    "typescript": "^5.7.3"
  }
}
